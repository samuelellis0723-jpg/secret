# 📝 Entregable 1: Bitácora de Prompts (Vibe Coding)

**Proyecto:** SECRET — Gestor de Reservas para Manicura  
**Metodología:** Vibe Coding (*Describe → Genera → Revisa → Prueba → Refina*)

---

## 📊 Tabla de Registro de Prompts por Fases de Desarrollo

| Fase | Prompt que escribiste | ¿Qué generó la IA? | ¿Qué revisaste / corregiste? |
| :--- | :--- | :--- | :--- |
| **Fase 1: Estructura & UI Base** | *"Crea la estructura inicial de la aplicación React con Vite y enrutamiento con React Router. Incluye las páginas públicas de catálogo de servicios `/reservar`, inicio de sesión `/login` y el layout base para el cliente."* | Generó el árbol de archivos en `src/`, configuración de React Router en `App.jsx`, maquetado inicial en JSX con estilos base en Tailwind/CSS y componentes navegables. | Corregí la estructura de carpetas para alinearte a reglas modulares (`pages/client/` y `components/`), eliminé importaciones relativas profundas (`../../../`) y ajusté la paleta de colores para el tema luxury por defecto. |
| **Fase 1: Estructura & UI Base** | *"Crea el componente modal de nueva reserva `NewReservationModal.jsx` con formulario por pasos (servicio, modalidad local/domicilio, fecha y hora)."* | Generó un componente modal React con estado interno para los pasos del wizard y los inputs de formulario. | Ajusté los nombres de las props a `camelCase`, corregí la accesibilidad del modal (cierre con tecla `Esc` y clic fuera del overlay) y validé que los campos requeridos fueran obligatorios. |
| **Fase 2: Funcionalidad Central** | *"Crea la API REST en Node.js/Express con conexión a PostgreSQL para consultar el catálogo de servicios (`GET /api/servicios`) y registrar citas (`POST /api/reservas`)."* | Generó el servidor Express, el archivo de conexión a PostgreSQL (`db.js`) y las rutas CRUD principales con consultas SQL tipo `INSERT INTO citas...`. | Revisé y corregí las validaciones de entrada en la petición `POST`, asegurando que la fecha y hora no estuvieran vacías y añadiendo tipos de datos correctos (`TIMESTAMP` en SQL). |
| **Fase 2: Funcionalidad Central** | *"Implementa la lógica del motor de disponibilidad para que al seleccionar una fecha sólo se muestren las franjas horarias no reservadas ni bloqueadas."* | Generó una función utilitaria que calcula los bloques de tiempo operativos de 8:00 AM a 6:00 PM y filtra los horarios ya registrados en la base de datos. | Corregí un error en la comparación de zonas horarias (`UTC` vs hora local de Costa Rica) que provocaba desplazar las citas en 6 horas y dejar libres horas ocupadas. |
| **Fase 3: Sistema & Pulido** | *"Crea la vista de administración `AdminDashboard.jsx` con un Inbox Atelier para cambiar el estado de las citas de `PENDIENTE` a `CONFIRMADA` o `CANCELADA` y refrescar el calendario."* | Generó la pantalla del panel de control con tarjetas de citas, botones de acción `Confirmar` y `Rechazar`, y manejadores de evento `handleStatusChange`. | **Error detectado:** La IA mutó el estado directamente en memoria sin persistir en la base de datos y no validó si el horario seguía disponible antes de pasar a `CONFIRMADA`. Agregué la llamada `PATCH` al servidor y el refresco automático de la lista de reservas. |
| **Fase 3: Sistema & Pulido** | *"Añade soporte para temas Dual (Modo Oscuro Privé y Modo Claro) con conmutador en el encabezado `AdminHeader`."* | Generó la lógica de contexto React (`ThemeContext`) y clases dinámicas en el nodo raíz `<html>` para alternar estilos. | Revisé los contrastes de color en modo oscuro para cumplir con accesibilidad (WCAG AA) y aseguré que la preferencia de tema se guardara en `localStorage`. |
